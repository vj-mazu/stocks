# PRODUCTION CONFIGURATION
# CRITICAL: Use the CONNECTION POOLER (Port 6543), NOT direct connection (Port 5432).
# Render and similar serverless platforms may experience ENETUNREACH errors with IPv6.
# The database.js config now forces IPv4 resolution as an additional safeguard.
DATABASE_URL=postgresql://postgres.knbgzutzgygdchrpgees:12345@aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres

# The URL of your deployed frontend (Vercel)
CLIENT_URL=https://millsoftware.vercel.app

# Node Environment
NODE_ENV=production

# IMPORTANT RENDER TIPS:
# 1. Always use the Supabase Connection Pooler (port 6543), not direct connection (5432)
# 2. If you still see ENETUNREACH errors, ensure your Supabase pooler URL uses IPv4
# 3. The database.js config automatically forces IPv4 resolution
