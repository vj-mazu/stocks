import React from 'react';
import Locations from './Locations';

const WarehouseManagement: React.FC = () => {
  return <Locations defaultTab="warehouse" hideTabs={true} />;
};

export default WarehouseManagement;
